name: Continuous Integration
run-name: ${{ github.actor }} is testing functionalities 🚀
on: [push]
jobs:
  Continuous-Integration:
    runs-on: ubuntu-latest
    steps:
      - run: ls
      - run: pip install -r requirements.txt
      - run: pytest
